#include<stdio.h>
 const int MAX=3;
int main(){
   int i;
    int var[]={10,100,200};
    ptr[i]=&var[i];
    for ( i = 0; i < MAX; i++)
    {
        printf("Value of var[%d]=%d\n",i,*ptr[i]);
    }
    
    return 0;
}